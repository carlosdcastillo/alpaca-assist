#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ ! -x "$repo_root/.venv/bin/python" ]]; then
  echo "Python environment is missing; run .agents/setup" >&2
  exit 1
fi

echo "Orb environment ready"
